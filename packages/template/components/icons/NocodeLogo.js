import React from 'react'

const NocodeLogo = ({
  size = 24,
}) => {
  return (
    <img
      style={{
        width: `${size}px`,
      }}
      src="images/favicon.png"
    />
  )
}

export default NocodeLogo
